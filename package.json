{
  "name": "ryanschmidt-design",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Personal portfolio site for Ryan Schmidt — Senior Product Designer.",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  },
  "scripts": {
    "seed-kb": "node scripts/seed-kb.js",
    "test:chat-api": "node scripts/test-chat-api.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate:kb": "vitest run -t 'KB integrity'"
  }
}
